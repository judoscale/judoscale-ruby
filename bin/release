#!/usr/bin/env bash
set -euo pipefail
IFS=$'\n\t'

for folder in ./rails-autoscale-*; do
    echo "-> $folder"
    cd $folder
    # can't call rake release since the tag is already created and pushed
    bundle exec rake build release:rubygem_push
    echo ""
    cd ..
done
